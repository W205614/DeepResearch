# DeepResearch 项目面经：从理解代码到面试表达

基线：2026-09-12，当前提交 `3afa0bec30694ed3f6741dfb0291ef68e449587a`。配套阅读：[导学-深度研究.md](导学-深度研究.md)。先看导学中的系统地图和一条请求，再练习本文。

证据说明：实现结论来自本次静态源码阅读；数字来自仓库 2026-09-11 审计，本次没有重新运行测试或评测。技术取舍是根据当前需求进行的分析，不是已经核实的作者决策史。第一人称答案是理解与表达练习，不能自动证明个人开发贡献；使用“主导、独立设计、优化”前必须补充自己的实际提交、调试过程和职责。

使用方式：每题先读“原理”，自己尝试回答，再核对“口播练习”，最后用“简短版”复述。主问题按 AI 应用后端面试准备优先级组织，高频度属于经验判断，不是招聘统计。全文 20 个主问题，5 组，每组另有 2 个追问。

## 1. 项目简介（简历可用）

DeepResearch 是面向中文研究任务的研究工作台，基于 Python、FastAPI 与 LangGraph，将网页和本地资料检索、证据筛选、报告生成及引用核验组织成可追踪流程。系统配套工作空间权限、资料安全处理、异步任务恢复和单机容器化部署，支持查看来源与研究过程。

定位是 AI 应用后端项目。当前是单机多服务部署，研究角色在一次 Worker 执行中共享进程及配置的模型端点，不代表分布式多模型协商系统。

## 2. 简历 bullet（按真实职责选用）

以下先提炼五个架构支柱：研究编排、检索治理、结果可信度、任务可靠性、访问与数据治理。个人负责模块尚未在本轮确认，因此采用项目实现表述；投递时仅保留自己确实参与、能够解释和举证的条目。

- **可追踪研究编排：** 针对研究步骤多、分支和补搜过程难以追踪的问题，项目以显式状态图组织路由、规划、双路检索、分析和报告核验，并通过角色工具许可与持久化事件记录执行过程，使步骤、输入输出类型及失败节点可以追溯；角色共享进程，事件不保存全部交接正文。
- **混合检索与缓存治理：** 针对字面术语和语义近义检索的差异，项目组合关键词与向量检索、加权排序和重复片段过滤，并以资料版本指纹约束查询缓存；历史固定集在 14 份资料、16 个标注问题上，Recall@5 从 96.88% 至 100%，不等同于最终报告准确率。
- **证据约束与质量评估：** 针对真实链接仍可能支持错误结论的问题，项目在结构化草稿后加入来源检查、重要事实规则和逐结论语义核验，仅保留通过检查的内容；历史完整研究评测为 29/30，引用支持率 99.66%，但关键题失败导致整体门禁不通过。
- **长任务可靠性：** 针对请求重试与后台进程中断，项目结合事务数据库、异步队列、请求幂等、执行版本与持久检查点，支持状态查询、失活恢复和失败任务人工处置；单机可控替身负载历史记录完成 386/386 个接受任务，不能据此承诺真实模型吞吐或生产 SLA。
- **访问与数据生命周期治理：** 针对共享工作空间中的权限和资料一致性问题，项目结合身份验证、资源归属检查、文件隔离扫描、索引版本与补偿清理，使未就绪或已删除资料受状态约束；跨存储清理仍是分步完成，不是统一分布式事务。

## 3. 面试问题与答案

### 第一组：项目整体与技术选型（最高优先级）

#### Q01. 请介绍项目，并说明一次研究请求经过哪些步骤。

**原理：** 按“产品目的→请求入口→后台处理→结果交付”回答。浏览器收到任务编号和 202 后，研究还在进行。普通对话可直接结束，深度研究才进入检索和报告链路。

**口播练习：**

我把这个项目理解为一个围绕证据生成研究报告的工作台。用户提出问题后，系统需要解决的不只是回答内容，还包括任务持续执行、来源能否核查以及过程能否看见。我沿代码追踪时，会先看接口如何校验身份、记录研究任务并投递给后台工作进程，再看研究流程如何判断普通对话或外部事实研究。需要研究时，系统制定计划，并行获取网页和本地资料，筛选证据、分析缺口，必要时有限补搜，最后生成草稿并逐条核验引用。浏览器通过持久化事件查看进度，通过查询接口获得报告。这个结构让执行过程可以解释，但报告仍可能存在语义错误，所以我不会把流程完成直接等同于研究结论完全正确。

**简短版：** 接口接收任务，后台按状态图检索和核验证据，前端查看步骤与报告；核心是研究流程和证据约束。

#### Q02. 为什么使用 LangGraph，而不是直接串几个函数或写一个大提示词？

**原理：** 当前流程存在分支、并行汇合、补搜回路和恢复需求，显式图有合理性。若只有“查一次资料→回答”，普通函数链可能更简单。框架提供编排基础设施，业务仍需实现限制和错误处理。[LangGraph 官方概览](https://docs.langchain.com/oss/python/langgraph/overview)

**口播练习：**

我判断这个选择是否合理，会先看流程复杂度，而不是先看框架知名度。这个项目既要区分聊天与研究，又要同时取得网页和本地证据，还要根据缺口决定是否补搜，因此已经不是简单的固定串行链路。我在代码中看到，各步骤通过共享状态交接，分支在指定位置汇合，补搜也有停止条件，并且执行状态能够保存。用显式图可以把这些控制关系集中表达，方便定位失败和理解恢复过程。代价是需要学习状态、检查点和图执行语义，调试也不一定比短函数容易。如果业务只是一次检索后生成回答，我会优先考虑简单函数链；当前选择的价值来自控制复杂流程，而不是自动提高模型智力或保证回答正确。

**简短版：** 选图编排是因为存在分支、回路和恢复需求；简单问答不一定需要它。

#### Q03. 为什么使用 Python 和 FastAPI，而不是 Java Spring Boot？

**原理：** 当前研究图、文档解析和检索组件集中在 Python 生态；接口处理大量外部 I/O。异步适合等待网络，不代表 CPU 计算自动变快，也不能直接得出 Python 性能高于 Java。[FastAPI 并发说明](https://fastapi.tiangolo.com/async/)

**口播练习：**

我会从项目的主要工作负载解释这个选择。这里大量时间用于等待模型、搜索、数据库和向量服务，同时研究图与文件解析本身已经使用 Python 组件。采用 Python 和 FastAPI 能让接口、异步调用和数据校验在同一技术体系内组织，减少跨语言适配。代码中通过异步等待释放事件循环，也把部分同步调用移到线程执行，避免阻塞其他请求。不过这不能推出 Python 比 Java 更快，切块和关键词计算等本地操作仍然会消耗处理器。如果团队已有成熟 Java 业务平台，我认为可以由 Spring Boot 处理账号和业务，Python 承担研究执行；是否拆分要比较部署、通信与维护成本，当前仓库没有完成两套方案的性能对照实验。

**简短版：** Python 便于衔接现有 AI 组件，异步接口适配外部 I/O；已有 Java 体系时可以重新划分边界。

#### Q04. 这个项目为什么不能只调用一次大模型？为什么选当前模型和搜索供应商？

**原理：** 一次模型回答无法天然获得新资料、任务恢复和资源权限。模型、Embedding、搜索承担不同职责，配置可替换不等于任意接口都兼容。仓库不能证明当前供应商在所有任务中最佳。

**口播练习：**

我认为这个项目需要多步处理，主要是因为它要求外部事实有依据，并且本地资料要受权限和状态约束。只调用一次模型，无法自动完成文件索引、可核查来源获取和长任务恢复。因此我会把生成模型、嵌入服务和搜索服务分开理解：模型组织计划和报告，嵌入用于语义检索，搜索发现候选来源，正文读取再确认可访问内容。当前代码通过适配器和配置连接这些能力，搜索也存在主备模式，但没有证据表明项目做过完整的供应商排名实验。面对为什么选某个模型，我会说明接口适配事实和验证需求，不编造价格或效果优势。更换供应商后，需要重新测结构化输出、否定数值题、时延和可验证来源比例。

**简短版：** 多步流程解决证据、权限和可靠性；供应商选择需要同题同预算验证，不能从配置推断最优。

#### 追问 A1. 是不是中间件越多，项目就越有技术含量？

**口播练习：**

我不会用组件数量判断项目质量。这个项目同时部署事务库、队列、向量库、对象存储和观测组件，是为了分别承担任务事实、投递、相似性检索、原件保存和问题定位，但每增加一个组件也会增加配置、资源和故障排查成本。我理解架构时会逐项问，如果去掉它，哪项需求无法满足，又能否用更简单的方案替代。比如在当前小规模资料库下，使用现有事务数据库的向量扩展值得评估，不能证明独立向量服务是唯一选择。我会保留真正服务于需求的能力，把精简部署作为可讨论的改进方向；现有形态可以证明组件协作实践，不能因为服务多就宣称达到生产级高可用。

**记忆点：** 每个组件都要回答“负责什么、代价是什么、能否简化”。

#### 追问 A2. 哪些内容是你个人完成的？

**口播练习：**

我会把项目已经具备的能力和我个人完成的工作分开说明。当前阅读能够确认研究编排、混合检索、权限和恢复机制存在，但仅凭代码存在，不能证明这些都是我独立设计的。如果被问到个人贡献，我会选择自己确实参与的一项改动，补充当时的问题现象、我阅读或修改的范围、采用方案的理由，以及对应的提交和验证记录。对于参考已有代码、借助工具完成或由他人实现的部分，我会如实说明协作边界，再解释自己理解和验证到了哪一层。尚未确认的经历不会用主导或从零设计来代替，这样后续追问能落在我真正掌握的决策和排错过程上。

**记忆点：** 此答案是职责边界模板，必须用自己的真实案例替换，不能直接冒认开发经历。

### 第二组：研究编排与可信回答（最高优先级）

#### Q05. 多 Agent 到底体现在哪里？是不是给同一个模型换几个提示词？

**原理：** 除提示词分工，还有角色工具许可、状态交接与事件记录；但角色共享进程和模型端点。工具包装器在角色作用域内校验，不是任意代码都无法绕过的隔离机制。

**口播练习：**

我会先限定这里多角色的含义。当前研究角色在同一次后台执行中共享进程和配置的模型端点，并不是多台服务器上的独立智能体。它比单纯换提示词多了几层约束：每个角色有明确职责和允许使用的工具，执行时进入对应作用域，调用受控工具前检查权限，各步骤通过结构化状态交接，并记录开始、结束和交接字段。比如规划角色主要调用模型，网页调研角色调用搜索和正文读取，这有助于减少职责混杂。不过这些约束依赖应用代码的受控入口，不能当作操作系统沙箱。它的实际结果是流程更容易追踪和检查权限，而不是证明多个角色会产生独立判断或必然超过单模型方案。

**简短版：** 是同进程的职责、工具和状态分工，具备应用层约束，但不是分布式智能体集群。

#### Q06. 并行分支怎样共享状态？怎样避免冲突和无限补搜？

**原理：** 网页与本地分支写不同结果字段，汇合后统一合并。补搜受模式、缺口、轮次、查询去重和搜索额度等条件限制。还需注意图递归上限与任务总超时不是同一种限制。

**口播练习：**

我会把状态写入和流程控制分开解释。并行检索时，网页分支与本地分支各自写自己的结果字段，后面的裁决步骤等待两路返回再合并候选证据，因此当前不是两个分支同时覆盖同一个证据列表。分析步骤会输出结论和仍然缺失的问题，补搜步骤再判断是否值得继续。代码中快速模式不进行补搜，深度模式也受最大轮次、搜索预算和已有查询去重约束，没有新查询就进入写作。此外还有任务总超时与图执行上限，避免控制逻辑异常时持续运行。我认为这个设计把循环变成有边界的尝试，但没有证明每次补搜都能增加有效资料；重复或无效结果仍需通过证据判断和最终报告中的局限说明来处理。

**简短版：** 分支写不同字段，汇合点统一处理；补搜由缺口驱动，并受去重、轮次、额度和超时限制。

#### Q07. 有引用是不是就能防止幻觉？具体如何核验？

**原理：** 来源存在只是第一关。还要检查该条来源是否支持主体、数值、年份、地区、范围和否定关系。当前先生成结构化草稿，再核验和有限修订，最后渲染报告。

**口播练习：**

我不会把有引用理解为已经消除幻觉。真实链接可能只提到相似话题，或者包含同一个数字但对应不同主体。当前实现先检查结论引用的来源是否属于已接受证据，再用确定性规则处理重要数字和来源条件，然后把每条结论及它实际引用的片段交给模型判断支持关系。没有通过时，系统允许修订一次，之后重新检查，最终只输出批准的结论。核验遗漏或重复的判断也不能默认通过。这样可以减少没有依据或引用错误的内容，但核验模型仍可能误判，尤其生成与核验共享模型端点时存在相关错误。我会把它描述为分层降低风险，并结合图外评测和人工原文核查评价效果，而不会承诺答案百分之百正确。

**简短版：** 来源编号检查、规则检查和语义支持检查是三层；都有引用仍可能存在错误。

#### Q08. 为什么用 Pydantic 和结构化输出？JSON 合法是否足够？

**原理：** 模型响应当前使用 JSON object 格式要求、提示词中的 schema 和本地 Pydantic 校验。并不是模型服务严格约束每个字段的 schema 解码。格式修复、格式重试、事实核验是不同阶段。

**口播练习：**

我理解结构化输出的主要作用，是让模型和普通程序之间有一个可检查的数据边界。计划需要问题和查询，结论需要文本和来源编号，如果全部返回自由文本，后续步骤就只能依赖脆弱的字符串解析。当前代码提供数据模型和字段约束，要求模型输出 JSON，再在本地验证；遇到少量格式问题会尝试受限修复，仍无效则重新生成一次或报错。不过合法 JSON 只能证明结构符合要求，不能证明事实和引用正确，也不能保证模型完成了用户全部目标。我会把格式校验用于阻止坏数据继续传播，把语义核验用于判断内容支持关系，再用评测检查整个任务效果，避免把其中一个环节的通过当成整体正确。

**简短版：** 结构化输出解决程序能否可靠消费，引用核验解决内容有没有依据，两者不能互相替代。

#### 追问 B1. 为什么总共 29/30 通过，整体质量门禁仍然不通过？

**口播练习：**

我会先说明这个数字来自仓库中上一轮固定研究评测，本次阅读没有重跑。质量门禁除了整体通过率，还要求关键题全部通过，因此一个关键错误不能被其他简单题的成绩抵消。记录里的失败场景是证据只说明任务完成情况，报告却把完成扩展为成功，后面又承认两者关系没有被确认。即使人数和覆盖内容没有明显缺失，这条语义扩展仍然没有得到引用支持。我认为这个失败有价值，因为它直接说明模型核验还会漏检。面试中我会保留失败结果，解释问题类型和下一步验证计划，例如增加类似边界反例并进行人工复核，而不是只报告总体百分比或反复重跑挑一个好成绩。

**记忆点：** 关键风险不能被平均分掩盖；该历史失败题名在源码索引中定位。

#### 追问 B2. 外部资料里写着“忽略前面的指令”，系统如何处理？

**口播练习：**

我会把这看作不可信内容试图改变执行规则的问题，而不只是一个特殊句子。当前模型提示明确把网页、文件和历史摘要中的命令视为资料，角色又通过受控工具许可限制行为范围，后续生成结果还要经过结构和引用检查。这些措施使资料较难直接升级成系统要求，但我不会承诺提示词隔离能够彻底防住注入，因为模型仍然可能被内容影响。文件病毒扫描解决的是另一类风险，也不能识别所有恶意语言指令。我认为验证时应当构造诱导调用工具、泄露上下文和污染报告结论的样本，分别观察工具权限和输出支持关系。只有经过这些验证，才能具体说明哪类攻击被阻断，哪类仍需要改进。

**记忆点：** 提示注入、病毒扫描和工具权限是不同防线，不应混为一谈。

### 第三组：RAG 与数据一致性（高优先级）

#### Q09. 一份 PDF 如何变成能被研究流程引用的资料？

**原理：** 文件验证→隔离存储→扫描→正文/图像提取→带定位切块→嵌入→索引与元数据更新→可检索。扫描和原件处理发生在上传流程中，后续索引交给后台，不能说所有处理都异步。

**口播练习：**

我会沿资料生命周期来解释，而不是直接从向量数据库开始。上传后首先检查文件大小、类型和签名，并把原件放入隔离区域，扫描通过后才进入后续索引流程。对于能够提取文字的 PDF，系统保留页码等定位，再把长内容切成有重叠的片段；可处理的图片会通过视觉模型提取信息。随后计算片段向量、写入检索数据，并在元数据状态确认可用后允许研究查询。查询命中的片段会连同文档和位置变成候选证据，仍需经过相关性筛选和最终结论核验。这条链路的结果是报告可以回溯到资料位置，但扫描 PDF、复杂版面和视觉提取都有边界，我不会说所有上传格式都能无损理解。

**简短版：** 文件先安全处理，再切块索引；检索命中还只是候选证据，需要后续核验。

#### Q10. 为什么选 Milvus，而不是 pgvector、FAISS 或 Chroma？

**原理：** 当前使用独立向量服务，按工作空间字段过滤，并在 SQL 层校验有效片段。小规模数据不足以证明独立向量数据库必要；pgvector 可将向量能力放入 PostgreSQL，应按成本与需求评估。[pgvector 官方仓库](https://github.com/pgvector/pgvector)、[Milvus 多租户说明](https://milvus.io/docs/multi_tenancy.md)

**口播练习：**

我会先承认当前仓库没有证明 Milvus 在这个数据规模下不可替代。它现在承担独立向量检索服务，应用传入工作空间过滤条件，再结合事务数据库中的有效片段处理结果，这让向量能力和任务元数据有清晰分工。但额外服务也带来资源占用、部署和跨存储一致性成本。对于已经使用 PostgreSQL 的小型项目，我认为向量扩展是值得对照的方案，可以减少组件；本地原型也可以考虑更轻的索引工具。我的选型回答会围绕权限过滤、更新删除、检索质量、运维和故障恢复展开，而不会虚构百万数据或性能对比成绩。真正决定替换前，应使用同一语料和查询测量质量、延迟与维护成本。

**简短版：** Milvus 是当前独立向量服务方案；规模小且已有 PostgreSQL 时，pgvector 有合理的简化价值。

#### Q11. 混合检索具体怎么做？为什么不只使用向量检索？

**原理：** 当前是 BM25 归一化分数与非负向量分数按 0.35/0.65 加权，再按词项集合相似度去重。不是 RRF，也没有模型重排。中文词法处理是单字切分，权重和切词都有改进空间。

**口播练习：**

我理解混合检索要解决的是两类相关性并不完全一致。向量有助于找到表达不同但语义接近的内容，关键词匹配则可能更直接命中特定术语、名称和编号。当前代码先分别计算词法和向量分数，对词法分数做归一化，多查询取各自较高分数，再固定加权排序并过滤高度重复片段。这样能让进入模型的资料兼顾语义和字面信息，但固定权重不是学习出来的最优值，中文单字处理也比较粗。我会准确称它为加权融合与重复过滤，不称为已经实现复杂重排。仓库小规模固定集记录了检索改善，但还需要扩大未见查询，并对分词、权重和替代排序做消融，才能评价泛化效果。

**简短版：** 词法和语义互补，当前是固定权重融合加去重；效果证据只覆盖既有固定集。

#### Q12. 缓存怎么失效？资料删除时，正在执行的索引会不会把它重新写回来？

**原理：** 缓存键含可用资料的内容及索引版本指纹。删除先更新 SQL 状态和版本，索引发布前再核对版本；跨存储删除使用持久化清理记录补偿。避免旧任务复活不等于所有并发读取立即撤回。

**口播练习：**

我会把缓存正确性和后台索引竞态放在资料版本上理解。当前查询结果的缓存不仅关联查询，还关联工作空间以及可用资料的内容和索引版本，因此资料集合变化后，旧结果不会直接用相同键命中。删除资料时，系统先在事务数据库中标记删除、推进版本并移除有效片段，再处理向量和原件清理。索引任务在发布结果前检查文档状态和版本，旧任务不能把已经删除或重建过的资料恢复成可用。跨存储清理失败时，记录会保留等待补偿。这是有权威状态的分步一致性治理，而不是三个存储共享事务。对于已经开始的查询以及旧事件，我仍会单独讨论可见性边界，不宣称全链路瞬时撤回。

**简短版：** 缓存绑定资料版本，索引发布校验版本，删除先撤销可用状态再补偿物理清理。

#### 追问 C1. 向量数据库故障时，会自动退化为纯 BM25 吗？

**口播练习：**

我核对当前检索函数后，不会说它已经具备独立的纯关键词降级。虽然本地流程先计算词法分数，但后续嵌入或向量请求如果抛出服务错误，整个本地检索会向上返回异常，由研究图记录警告并继续使用其他可用来源。也就是说，计算过关键词分数不等于异常时一定会把结果交出去。如果要改进，我会考虑把两种检索通道的失败分开处理，至少保留可用的词法结果，并明确标注降级状态。不过这样还需要验证排序阈值、缓存键、租户过滤和告警是否一致，不能只增加一个异常捕获就声称完成可靠降级。这是当前实现的边界和候选改进，不是已经完成的能力。

**记忆点：** 图层使用其他来源 ≠ 本地检索自动返回 BM25。

#### 追问 C2. 本地文件明确写了一个数字，为什么报告可能还是拒绝引用？

**口播练习：**

我会区分资料是否检索到和结论是否满足证据规则。当前重要事实规则先要求有正文或本地文档，但后面还要求两个网页域名或者已经配置的可信来源标签。普通本地片段没有网页域名，也通常没有该标签，所以即使数字逐字存在，也可能无法通过后面的条件。这说明当前规则对纯本地资料场景比较保守，不能简单解释成模型没有读懂文件。如果需要调整，我会先明确哪些本地资料可以建立可信来源身份，以及同一文件的多个片段能否算独立支持，再增加针对数字、主体和冲突的测试。直接把所有上传资料默认视为权威会引入新的风险，因此我会把这个问题作为规则适配边界来说明。

**记忆点：** 来源规则可能过严，不能编造“本地文件天然可信”的替代解释。

### 第四组：长任务、并发与恢复（高优先级）

#### Q13. 为什么要 Redis 和 Worker？接口里启动一个异步任务不行吗？

**原理：** 异步语法解决等待期间的调度，不解决进程退出后的任务存活。默认部署将任务事实写 SQL，Redis 投递给 ARQ Worker。数据库和队列之间并非原子提交，而是持久状态加重新投递补偿。[ARQ 官方文档](https://arq-docs.helpmanual.io/)

**口播练习：**

我会先区分异步和持久化。接口里创建异步任务可以让响应更快返回，但任务仍依附当前进程，进程退出时不能仅靠这个对象恢复。研究任务持续时间长，还需要查询、取消和重启后继续，所以默认部署先把任务写到事务数据库，再通过 Redis 投递给独立 Worker 执行。数据库是任务事实来源，队列主要负责通知谁来做，投递失败后仍能根据数据库中的待处理记录重新投递。这个设计提高了恢复的可解释性，但增加了队列和状态协调成本，也没有数据库与队列的共同事务。我会用投递失败和重复投递用例检查补偿机制，而不是只因为接入队列就宣称不会丢任务或重复调用外部服务。

**简短版：** 异步不等于任务持久化；SQL 保存事实，队列驱动后台执行，补偿处理投递间隙。

#### Q14. 怎样防止用户重复提交和多个 Worker 重复执行？

**原理：** 请求幂等、会话并发约束、任务抢占、执行版本分别处理不同层面。内存锁仅约束当前进程；PostgreSQL 锁和数据库条件更新才支持跨进程协调。

**口播练习：**

我会按不同的重复来源分层解释。客户端因为网络不确定重发请求时，服务端在工作空间范围内查找相同请求编号，同一内容返回已有任务，不同内容则返回冲突。数据库还限制同一会话同时存在的活跃研究，避免会话状态混乱。到了执行端，Worker 必须通过带状态条件的更新抢占任务，抢不到就不能继续正常执行；每次执行还有递增版本，完成、失败和心跳写回时检查版本，减少旧执行者覆盖新状态的问题。进程内锁只是其中一层，不能单独解决多进程竞争。我认为这些机制可以约束任务事实和终态，但对已经发出的模型或搜索请求，仍然不能承诺外部服务只执行一次。

**简短版：** 请求编号防重复创建，数据库约束与抢占防并发冲突，尝试版本保护关键写回。

#### Q15. Worker 崩溃后怎样恢复？检查点是否意味着任何地方都能无损续跑？

**原理：** 运行任务定期刷新心跳，恢复循环将失活任务标为中断并重新投递。图检查点用 Run ID 定位；恢复可能重做未完成节点，外部调用不是回滚事务。

**口播练习：**

我会把恢复拆成发现失活、重新调度和继续执行三步。运行中的任务周期更新心跳，恢复循环发现心跳过期后，通过条件更新把任务变为中断并重新投递。Worker 再根据这次研究对应的持久检查点恢复图状态，如果没有可用检查点，就重新建立初始状态。这里能复用已经保存的流程进度，但不能保证从任意语句精确续跑，未完成节点可能再次调用外部服务。当前心跳间隔和失活阈值只是配置与代码条件，实际恢复还包含检查周期、排队和执行耗时。我会把历史单次恢复时间作为演练观察，不承诺固定时间内无损恢复，更不会把检查点理解为对外部模型费用和副作用的完整事务保护。

**简短版：** 心跳发现失活，SQL 状态驱动重投递，检查点恢复图进度；未完成节点仍可能重做。

#### Q16. 为什么用 SSE，而不是 WebSocket？断线重连会不会重新执行研究？

**原理：** 主要交互是服务器单向发送进度，控制动作走普通 HTTP。当前通过 Fetch 读取 SSE，方便携带鉴权头；客户端用事件游标去重。后端事件读取仍包含数据库轮询。[MDN SSE](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events)

**口播练习：**

我认为当前场景用 SSE 的依据是交互方向比较明确：用户提交研究后主要接收步骤和状态，取消、恢复等动作可以单独调用 HTTP 接口，不需要为了这些动作维护双向消息协议。实现中浏览器通过 Fetch 读取事件流，保留已处理的事件编号，断线后从游标继续请求，重复编号不会再次展示。服务端读取已经持久化的事件，所以重连只是重新获取进度，不会重新启动研究图。它目前也不是模型逐字输出，而是节点和业务事件推送。代价是长连接、代理配置和数据库事件读取仍要管理，后端还存在定期查询。我会通过断网、数据分片和终态补查验证体验，而不把采用 SSE 本身当成完整的可靠交付保证。

**简短版：** 单向进度适合 SSE；重连重放的是持久事件，不是重新执行任务，也不是模型 Token 流。

#### 追问 D1. 取消、自动重试、人工恢复和死信是什么关系？

**口播练习：**

我会先说明这些机制对应不同原因。用户取消是主动停止，系统先保存取消状态，再尝试中止实际执行，晚到的结果不能随意覆盖这个状态。外部网络调用的有限重试只处理部分暂时性错误，不代表整个任务失败后自动无限重跑。当前队列配置没有启用通用自动重试，失活恢复主要由任务心跳和持久记录驱动。执行失败会有持久的失败处置记录，管理员可以发起恢复，但恢复按钮成功只代表重新进入流程，最后还可能失败或资料不足。我会检查实际终态后再说明恢复结果，避免把已调度当成已成功。这种区分也便于控制成本和定位错误究竟来自依赖抖动、用户操作还是研究本身。

**记忆点：** 持久化处置记录不是 Redis 原生死信队列；恢复申请成功不是报告成功。

#### 追问 D2. 能不能说系统实现了 exactly-once？

**口播练习：**

我不会这样概括当前系统。请求幂等、任务抢占和尝试版本能够降低重复任务以及旧结果覆盖的风险，但一次模型请求可能已经被对方处理，而本地在保存结果前崩溃，恢复后仍会重新请求。类似地，图检查点、事件、队列和外部服务之间没有一个覆盖所有副作用的共同事务。当前比较准确的表述是对任务状态和关键终态做了重复与陈旧写入防护，执行过程仍可能重放。如果以后要进一步强化，我会先列出每一种副作用，判断是否能使用业务幂等键、去重记录或更完整的执行版本校验，再分别设计失败窗口测试。没有这些证据，就不能从一次负载中没出现重复终态推导出端到端恰好一次。

**记忆点：** 某次测试无重复终态 ≠ 所有外部副作用恰好一次。

### 第五组：权限、安全、验证与工程取舍（高优先级）

#### Q17. 登录认证和工作空间权限怎么做？为什么选择 OIDC？

**原理：** Keycloak 管账号登录，后端验证签名、有效期、签发方和受众，并按成员关系与角色授权。资源查询仍要约束归属。前端解析令牌用于显示，不替代后端认证。

**口播练习：**

我会把认证解释为确认是谁，把授权解释为这个身份可以访问什么。当前浏览器使用授权码和 PKCE 完成登录，后端验证访问令牌后取得账号主体，再查询工作空间成员关系与角色。真正读取研究、文档或导出数据时，还要检查资源属于该空间，所以知道一个资源编号并不等于有权限。采用标准身份流程，可以把密码和账号管理交给身份服务，让业务重点放在资源授权上，但自托管身份服务也增加运维成本。这里有个边界需要说明：成员关系主要在请求入口重新检查，已经建立的事件流和正在执行的任务没有逐步骤撤权机制。我不会把新请求拒绝访问扩大成所有既有连接立即停止。

**简短版：** OIDC 确认账号身份，数据库成员关系与资源归属决定访问权限；即时撤权仍有边界。

#### Q18. 会话历史、个人偏好和语义记忆如何隔离？为什么不能直接当证据？

**原理：** 个人记忆按账号主体，研究历史和报告记忆受工作空间与 Thread 约束。上下文有数量和长度限制。历史报告可能已过时或自身有误，必须和新检索证据分开。

**口播练习：**

我理解这里的记忆至少有三个范围。个人偏好与设定属于账号，不应因为两个人加入同一工作空间就相互读取；连续对话历史属于当前会话，系统只取有限的问题索引和最近报告；保存的报告语义记忆还受会话与相关性约束。这样既能延续问题，又避免把其他会话内容混进来。更重要的是，这些历史内容只用于理解需求，不能自动作为本次研究的事实来源，因为旧报告可能过时，也可能包含当时没有发现的错误。如果把历史回答重新当作证据，错误会不断自我强化。我会根据当前问题重新检索并核验外部事实，同时检查数据库查询和向量过滤是否一致，而不是只依赖提示词说请勿串会话。

**简短版：** 私人记忆按账号，会话历史按空间和会话；历史有助理解，但事实需要重新核查。

#### Q19. 文件上传和网页抓取有哪些安全控制？还有什么限制？

**原理：** 上传检查大小、签名和解压约束，扫描失败拒绝进入索引。抓取校验 URL、DNS、跳转和体积，并固定已验证地址。对象复制/删除不是原子移动，HTML 清洗和提示注入防御也属于不同层。

**口播练习：**

我会按输入通道解释。文件上传先校验体积、类型和真实签名，对压缩格式还限制展开规模，原件进入隔离区并完成扫描后才允许索引，扫描服务不可用时不会直接放行。网页抓取则防止服务端被诱导访问内网，检查协议、端口和解析地址，对跳转重新验证，并限制响应大小，连接使用已经验证的目标地址。前端展示报告也进行 HTML 清洗，减少恶意内容直接执行的风险。不过这些机制不能证明所有资料可信，病毒扫描不处理所有提示注入，正文可读也不代表一手来源。我还会注意对象存储移动是复制后删除，涉及多个步骤，不能照搬文档中的原子移动说法来承诺跨存储事务。

**简短版：** 上传安全、网络出口、展示清洗和提示注入分层处理；每层都要说明实际覆盖范围。

#### Q20. 怎样证明项目有效？有哪些不足，下一步优先改什么？

**原理：** 将实现测试、组件集成、检索评测、答案质量、负载与恢复分开。当前证据不足以宣称生产采用、商业收益、通用准确率或多副本高可用。改进优先级应由失败和需求驱动。

**口播练习：**

我会把验证分成几个互不替代的层次。单测检查路由、权限、状态和引用规则，真实组件测试检查存储与恢复，检索评测检查相关资料是否排在前面，完整研究评测才进一步检查结论支持和应答覆盖。仓库历史记录中，固定研究题并非全部通过，关键语义题失败仍然使门禁不通过；负载测试又使用了可控模型和搜索替身，不能据此推导真实模型吞吐。我认为下一步应优先处理已暴露的语义漏检，并扩大未见题与人工复核，再根据产品需求评估纯本地证据规则、向量故障降级和撤权边界。当前单机架构也不证明高可用，我会先建立可复现的失败与验收标准，再讨论增加组件或扩容。

**简短版：** 分层验证，保留失败和替身边界；先解决已知质量问题，再按需求扩展工程能力。

#### 追问 E1. Recall@5 为 100%，为什么不能说回答准确率为 100%？

**口播练习：**

我会先解释这个指标的分母是什么。检索召回率衡量人工标注的相关资料有多少出现在前几个结果中，它不直接判断最终回答里的每句话。即使相关文件全部被检索到，切块可能缺少上下文，模型可能漏读某段，也可能把否定或数字主体理解错，所以答案仍然会出错。当前历史结果还只覆盖小规模冻结语料与查询，并不是对所有研究主题的保证。我会把检索指标与结论支持率、预期事实覆盖率分别展示，说明它们各自的评测对象和样本。若要证明回答质量，需要使用独立的答案检查和原文核对，而不是把一个百分比换个名称放进简历，这样才能定位错误到底发生在召回还是生成环节。

**记忆点：** 检索评测、答案评测、系统负载各有分母，不能互换名称。

#### 追问 E2. 如果任务变慢，怎么定位？为什么不直接上 Kubernetes？

**口播练习：**

我会先测慢发生在哪一段，而不是立即增加部署复杂度。一次研究可能慢在排队、外部模型、网页正文抓取、嵌入、向量检索或本地处理，项目已有节点耗时、调用计数和链路观测，可以先区分依赖等待与本地资源问题。比如外部模型响应慢，增加应用副本可能只会提高并发请求和费用，并不会缩短单次调用；如果是关键词语料加载占用内存，应该先检查资料规模和缓存策略。当前 Compose 主要用于单机复现，是否采用更复杂的编排要取决于多副本、资源调度和运维需求。我会先复现瓶颈、给出测量和改进目标，再决定优化代码、调整并发还是改变部署，而不会把平台名称当作性能方案。

**记忆点：** 先定位等待、计算或资源瓶颈，再选择优化或扩容。

## 4. 源码证据索引

下表集中列出内部路径、函数和字段。建议使用编辑器搜索符号，不依赖可能随修改漂移的行号。路径相对于项目根目录。

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 请求与数据契约 | `backend/domain/models.py`：`RunRequest`、`Plan`、`Evidence`、`Claim`、`ResearchState` | Q01、Q06、Q08 |
| 前端提交 | `frontend/src/App.vue`：`send`、`selectRun`、`refreshRun` | Q01、Q16 |
| API 接受任务 | `backend/api/app.py`：`start_run`，`POST /api/research/runs` | Q01、Q13 |
| 图的分支、汇合与循环 | `backend/research/graph.py`：`ResearchGraph.build`、`router`、`planner`、`reflect` | Q02、Q06 |
| 角色与工具许可 | `backend/research/agents.py`：`AGENTS`、`AgentRuntime.activate/require/handoff`；`tests/test_agents.py` | Q05、B2 |
| 当前协程作用域 | `backend/research/agents.py`：`ContextVar`，退出时 `reset`；无作用域时 `require` 直接返回 | Q05 |
| 模型结构输出 | `backend/infrastructure/providers.py`：`Providers.structured`、`post`、`remove_json_trailing_commas` | Q03、Q04、Q08 |
| 搜索服务与主备 | `backend/infrastructure/providers.py`：`search`、`bocha_search`、`deepseek_search` | Q04 |
| 网页并发实际边界 | `backend/research/graph.py`：`web_scout` 的查询循环、正文 `asyncio.gather`、`fetch_gate` | Q06 |
| 证据与报告核验 | `backend/research/graph.py`：`judge`、`analyst`、`writer`、`check_draft`、`validator`、`supported_indices` | Q07、B1 |
| 数字与来源规则 | `backend/research/evidence_policy.py`：`validate_claim`、`requires_strong_evidence` | Q07、C2 |
| 文件与切块 | `backend/services/documents.py`：`validate_upload`、`semantic_chunks`、`split_document`、`split_for_index` | Q09 |
| 扫描与隔离 | `backend/services/documents.py`：`_add`；`backend/core/clamav.py`；`tests/test_document_security.py` | Q09、Q19 |
| 对象移动非原子 | `backend/infrastructure/object_store.py`：`ObjectStore.move` 的 `copy_object` 后 `delete_object` | Q19 |
| 混合检索和缓存 | `backend/services/documents.py`：`tokens`、`search`、`_corpora`、`corpus_hash`、`invalidate_search_cache` | Q11、Q12 |
| 固定排序与去重 | `backend/services/documents.py`：`0.65 * vector + 0.35 * lexical`，Jaccard 阈值 `0.82` | Q11 |
| 向量隔离及配置指纹 | `backend/infrastructure/vectors.py`：`ensure`、`fingerprint`、`search` | Q10、Q18 |
| 本地检索故障路径 | `backend/services/documents.py`：`retrieve_query`；`backend/research/graph.py`：`local_scout` | C1 |
| 索引与删除并发 | `backend/services/documents.py`：`ingest`、`delete`、`cleanup_pending`、`index_version`；`tests/test_consistency.py` | Q12 |
| 任务创建与幂等 | `backend/services/runtime.py`：`Runtime.create`、`client_request_id`；`backend/core/db.py`：唯一约束与 `one_active_thread` | Q13、Q14 |
| 跨进程锁 | `backend/core/postgres.py`：`guard`、`pg_advisory_lock`；`backend/core/consistency.py`：`local_guard` | Q14 |
| 任务投递与恢复 | `backend/services/runtime.py`：`schedule`、`run_job_id`、`recover_queued_runs` | Q13、Q15 |
| 抢占与尝试隔离 | `backend/services/runtime.py`：`execute_run`、`attempt_count` 条件更新；`tests/test_consistency.py`：`test_old_attempt_cannot_complete_or_fail_new_attempt` | Q14、D2 |
| PostgreSQL 检查点 | `backend/services/runtime.py`：`start` 中 `AsyncPostgresSaver`；`execute_run` 的图配置将 `thread_id` 设为 `run_id` | Q15 |
| 心跳与失活 | `backend/services/runtime.py`：`heartbeat`、`recover_stale_runs_loop`、`recover_stale_runs` | Q15 |
| 队列重试配置 | `backend/worker.py`：`WorkerSettings`、`retry_jobs=False` | D1 |
| 取消与死信 | `backend/services/runtime.py`：`cancel`、`fail`、`resume`、`dead_letters`、`recover_dead_letter` | D1 |
| SSE 重放与去重 | `backend/api/app.py`：`events`；`frontend/src/api.ts`：`subscribe`、`parseFrame` | Q16 |
| JWT 验证 | `backend/core/auth.py`：`TokenVerifier.verify`、`Principal.subject` | Q17 |
| 授权与资源归属 | `backend/api/app.py`：`identity`、`researcher`、`administrator`；`backend/core/postgres.py`：`owned_run` | Q17 |
| OIDC 登录前端 | `frontend/src/auth.ts`：`startAuthorization`、`completeLogin` | Q17 |
| 当前前端工作空间边界 | `frontend/src/App.vue`：`refreshWorkspace` 取列表首项；`frontend/src/api.ts`：`headers` | Q17；配套导学 6.1 |
| 历史与个人记忆 | `backend/services/runtime.py`：`context`、`save_summary`；`owner_subject`、`created_by`、`thread_id` | Q18 |
| SSRF 防护 | `backend/core/security.py`：`canonical_url`、`public_addresses`、`fetch_text`；`tests/test_evidence.py` | Q19 |
| 前端 HTML 清洗 | `frontend/src/App.vue`：`renderReport`、`DOMPurify.sanitize` | Q19 |
| 质量门禁 | `backend/quality_gate.py`；`scripts/evaluate_research.py`；`eval/research_quality_cases.json` 的 `negation-2` | B1、Q20 |
| 历史评测证据 | `docs/resume-audit-20260911.md`、`eval/audit-20260911.json`、`eval/benchmark_results.md` | Q20、E1 |
| 分层观测 | `backend/core/metrics.py`、`backend/core/observability.py`、`observability/` | E2 |
| 部署与 CI | `compose.yaml`、`.github/workflows/ci.yml`、`.github/workflows/answer-quality.yml`、`docs/operations.md` | A1、Q20、E2 |

## 附录 A：面试前十分钟复习

| 容易说过头的表述 | 当前更准确的说法 |
| --- | --- |
| 十个独立智能体分布式协作 | 十个已注册职责角色，包含聊天与路由；单路径不执行全部角色 |
| 搜索供应商都并行执行 | 网页与本地分支并行；搜索供应商按配置选择，自动模式主备回退 |
| SSE 流式生成模型文字 | 当前是持久化业务事件流，展示研究进度 |
| JSON 合法所以不会幻觉 | JSON 校验解决结构，语义核验仍可能漏检 |
| 本地资料天然可信 | 当前重要事实规则还要求网页域名或可信标签，纯本地可能被拒绝 |
| Milvus 不可用就自动纯关键词 | 当前本地检索异常上抛，图层使用其他可用来源 |
| 混合检索就是 RRF 或重排模型 | 当前是固定加权与重复片段过滤 |
| MinIO 原子移动 | 当前复制后删除，不是跨对象原子操作 |
| Worker 崩溃无损续跑 | 持久检查点恢复，未完成节点可能重做 |
| 所有失败都会自动重试 | 网络有限重试、失活恢复与人工恢复分别处理 |
| 实现端到端恰好一次 | 关键任务状态有重复与旧写入防护，外部调用仍可能重放 |
| Recall@5 100% 代表回答全对 | 只是特定冻结集检索指标 |
| 29/30 说明质量门禁通过 | 关键题失败，整体门禁未通过 |
| 53.50 秒是恢复 SLA | 历史单机单次演练观察 |
| 接入 OIDC 就自动多租户安全 | 仍需逐资源授权，既有连接即时撤权尚无保证 |

## 附录 B：个人案例填写卡

不要只背架构。选一项自己真实处理过的问题，补齐下面内容，才形成可追问的个人 STAR 案例：

| 项目 | 自己填写 |
| --- | --- |
| 具体场景 | 哪个操作或输入触发了问题？ |
| 原有现象 | 实际返回什么、哪个状态不对、什么数据缺失？ |
| 个人任务 | 自己负责诊断、实现、测试还是文档？其他人/工具做了什么？ |
| 判断依据 | 哪段源码、日志、测试或提交支持归因？ |
| 备选与取舍 | 为什么选择该方案，牺牲什么，为什么未选另一个？ |
| 实际动作 | 自己具体改了哪些逻辑或增加了什么验证？ |
| 结果 | 哪项验证通过，是否有反例、剩余问题和范围限制？ |
| 证据 | 提交、测试输出、复现步骤，均用真实记录填写 |

## 附录 C：后续技能交接摘要

**交给 /great-resume：** 项目名称为 DeepResearch 企业研究工作台，建议面向 AI 应用后端岗位；个人负责范围待确认。候选动作包括显式研究图、混合检索、分层引用校验、任务恢复和资料治理。实现可查源码索引，数字可查 2026-09-11 审计；不能把历史结果写成今天复测，不把项目能力自动改成个人主导。候选简介与五条 bullet 见本文前两节。

**交给 /interview：** 优先检查个人贡献、指标分母、共享模型角色边界、图状态并发、幂等与外部副作用、纯本地证据限制、索引删除竞态及即时撤权。Ownership Claim 未确认；Metric Claim 必须附样本和替身条件；Architecture Claim 需解释源码及故障窗口；Result Claim 没有商业上线、用户采用或生产 SLA 证据。模拟面试宜一次一问，并围绕答不清的边界追问。
