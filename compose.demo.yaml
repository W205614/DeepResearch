# Deprecated compatibility overlay. Fixed-data and development-identity runtime modes are no longer deployable.
# Offline fixtures remain available only to automated tests and evaluation commands.
services: {}
