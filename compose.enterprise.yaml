# Deprecated compatibility overlay. The default compose.yaml is the enterprise single-node demonstration.
# Keep this file temporarily so existing automation using -f compose.enterprise.yaml remains functional.
services: {}
