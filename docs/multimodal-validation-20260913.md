# 多模态验证记录（2026-09-13）

## 自动化回归

- Ruff 检查通过；后端 **158 passed、4 skipped**，耗时 91.73 秒。
- 4 项跳过为需要 `INTEGRATION=1` 和独立 `compose.verify.yaml` 的组件集成测试；不计为通过。
- 前端现有 **12 项测试通过**，类型检查和生产构建通过。
- 新增图片测试覆盖格式/损坏/动画/体积、上传者与工作空间隔离、安全扫描不可用、过期清理、
  多图纯图片提交、幂等顺序、原图删除、视觉故障、模糊图片、下游失败恢复及进程重启后的解析复用。
- 固定模型替身验证：网页无结果但图片可支持结论时仍能产出图片引用；图片文字中的命名指令不修改用户档案。
  检索空结果、全故障和预算耗尽分别得到预期状态。以上不是模型准确率评测。

## 真实接口与浏览器

- 使用程序生成的合成图片，不包含个人或业务资料。请求模型为 `deepseek-v4-flash-vision-exp`，
  接口返回 `deepseek-flash`；数字 12、7 和“未完成”否定关系检查通过。
- 浏览器通过本机 Keycloak 临时账号实际完成单图加文字提问，以及粘贴图片后追加第二张图片、
  不填文字直接发送。两次均为 `completed / vision`，各 **1 次模型调用、0 次搜索**。
- 实际验证上传扫描状态、图片预览、来源卡片、受控原图、切换新会话不沿用附件；未观察到浏览器错误日志。
- 浏览器使用的临时账号、凭据与应用内上传图片已清理；合成图片及脱敏摘要留在被忽略的 `.cache/vision-smoke/`。
- 真实浏览器验证针对看图分流；图文研究的引用闭环由上述固定替身回归覆盖，不宣称已经完成真实模型的全面图文研究评测。

## 本机容器与观测

- PostgreSQL 已迁移至 `0005_image_attachments`；API、Worker、Web 已构建更新并健康运行。
- Grafana 实际保存的仪表盘为 12 个面板，新面板编号为 7–12；通过只读查询面板记录核实。
- Prometheus 中已查询到真实视觉成功调用、耗时、token，及初始化的检索/兜底类别。
- Loki 中查到视觉调用日志，关联的 Tempo 追踪可读取；抽查未包含原图 Base64、测试文字或密钥字段。
- Prometheus 告警规则校验通过，共 4 条规则。
- Grafana 管理员凭据登录返回 401；未重置密码。面板加载和底层数据已核实，未宣称验证了 Grafana 网页登录或逐面板视觉展示。

边界：图片解读是模型输出，仍可能出现识别或解释偏差；“图中显示”不等于外部事实核实。
持久化前崩溃仍可能造成外部视觉调用重发；单机恢复测试不构成恢复 SLA 或恰好一次保证。
