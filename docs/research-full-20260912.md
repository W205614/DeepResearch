# 固定证据真实模型完整复测（2026-09-12）

术语边界修复后，使用原始 30 题进行一次完整运行：**30/30 全部通过**，引用支持率 **100%（297/297 条）**、应答覆盖率 **100%**，关键题失败数为 0，固定研究质量门禁通过。

结果见 [research-full-20260912.json](../eval/research-full-20260912.json)。这不是把定向 6/6 与历史 29/30 拼接，也未删除题目、更改预期事实或放宽评分门槛。2026-09-11 的失败保留在 [原审计](resume-audit-20260911.md) 与原始结果中。

- 模型：`deepseek-v4-flash`，thinking disabled，并发 2；实际执行研究图，图外另行评分。
- 代码基线：`a713a9bf721e508244efd233670cf378085773cb`，含尚未提交的 `backend/research/graph.py` 术语边界修复。运行文件 SHA-256：`9cc97a68f7ce470a6a7b012bfd4cba659db5537c4eacfd9a6c6867000e677301`。
- 题集 SHA-256：`b221029efcd4da0c6016dc16d38683cf080f9c8ab8d1213af907bbb25d17cfb7`；全量 30 个唯一 ID 与当前题集一致。
- `negation-2`：8/8 条引用结论获支持、预期事实覆盖率 100%；`conflict-4`：16/16 条获支持、预期事实覆盖率 100%。
- 日志出现两次可恢复的 JSON 格式错误（`single-2` 修订及 `negation-3` 规划），由现有请求重试机制处理；没有在完整运行结束后重跑失败题替换结果。

复现命令：

```powershell
$env:PYTHONIOENCODING='utf-8'
.venv/Scripts/python.exe scripts/evaluate_research.py --output .cache/terminology-full-20260912/results.json
```

原始报告与逐条判定保存在本地 `.cache/terminology-full-20260912/research-d763e25ffb0d4c69a0b23aec5230d832/`，公开结果只保留脱敏统计。

可采用表述：**固定证据真实模型评测 30/30 全部通过，图外模型评分的引用支持率和应答覆盖率均为 100%。**

边界：这是小规模合成固定证据题集上的单次运行，图外评分与生成使用同一模型，不是人工盲评，不代表通用准确率或每次运行必然通过。本次只完整复测固定研究质量；其他组件、性能和实时联网指标仍引用各自原有日期的独立记录。

## 后续 CI 验证

提交 `16c4125` 的[常规 CI](https://github.com/W205614/DeepResearch/actions/runs/34688336662)全部通过，但[真实模型质量门禁](https://github.com/W205614/DeepResearch/actions/runs/34688336680)失败：`negation-2` 最终 2 条结论获支持，预期事实覆盖率为 0；其余 29 题通过。这是独立远程运行，不覆盖上面的本地 30/30，也说明结果存在波动。旧 CI 未输出正文，不能确定事实在哪一步丢失。

随后补齐修订输入中的主题、计划、分析事实和已通过核验的索引，要求保留可回答事实；规划允许单点题只拆一个问题，优先提取原文主体、人数和状态。新增仅供内置合成题使用的失败正文诊断，禁止对实时网页、外部结果或自定义题集启用。本地 `negation-2`、`conflict-4` 2/2 通过，相关原有测试 47 项通过；远程完整结果以修复提交对应的工作流为准。

提交 `b646bc5` 的[第二轮 CI](https://github.com/W205614/DeepResearch/actions/runs/34688793690)中，代码质量、真实组件和企业冒烟通过，普通 Compose 冒烟暴露首次启动竞态：后端与 Worker 同时检查不存在的桶，一方创建成功后另一方的 CreateBucket 返回 `BucketAlreadyOwnedByYou`，被当成存储不可用而退出。修复为只在桶不存在时创建；若当前账号已拥有桶则重新 HEAD 验证访问。权限拒绝、其他账号占用或复核失败仍拒绝启动，新增回归覆盖这些分支。

第二轮[模型门禁](https://github.com/W205614/DeepResearch/actions/runs/34688793702)失败于 `negation-3`，新增正文诊断确认报告已明确写出甲组 26 人全部完成、乙组 28 人全部未完成，但覆盖评分返回 `[false, false]`，属于与预期事实不一致的评分误判。修正覆盖评分指令：以 `expected_facts` 为目标，检查整个正文，术语等价关系未确认不否定已陈述的原文事实；错误、遗漏或自相矛盾仍不覆盖。未改题目、预期事实、通过阈值或引用支持评分。冻结同一份 CI 报告重新评分得到支持率和覆盖率均 100%，不算重新生成报告通过。

真实模型反例检查：主体状态颠倒、仅说明术语而遗漏人数、正文自相矛盾三种变体均被整体门禁拒绝。前两种覆盖率为 0；矛盾变体的覆盖评分仍给出 100%，但引用支持率 50% 且命中禁止事实，因此整体失败。这保留了评分模型仍可能误判的边界，不能宣称提示修复使语义判定确定可靠。本地诊断结果位于 `.cache/ci-grader-20260912/`。

提交 `21d8e6a` 的[常规 CI](https://github.com/W205614/DeepResearch/actions/runs/34689008346)全部通过，但[模型门禁](https://github.com/W205614/DeepResearch/actions/runs/34689008361)的 `injection-4` 仅 2/3 条引用结论获支持。正文额外声称“上述命令式内容未被作为指令执行”，却引用资料原文来证明本系统行为。写作与核验提示因此补充：系统执行过程自述不能作为该资料支持的事实，来源不能证明系统执行或未执行某条命令；不改变资料中事实段落的可用性。

提交 `5f7c9e4` 的[常规 CI](https://github.com/W205614/DeepResearch/actions/runs/34689154782)与[模型质量门禁](https://github.com/W205614/DeepResearch/actions/runs/34689154810)均通过。后端 117 通过、4 跳过；独立回答和完整研究评测成功退出。上述执行过程引用边界的追加修复另在本地跑完 5 道注入题，5/5 通过；其提交后仍需执行对应 CI，不能沿用父提交成功状态。
