PostgreSQL schema is initialized by the application adapter for the current demo.
Before a multi-team deployment, replace this bootstrap with generated Alembic revisions and run `alembic upgrade head` as a dedicated migration job.
